# 🏨 เว็บไซต์จองที่พักออนไลน์ - Minimal Design

เว็บไซต์จองที่พักออนไลน์สไตล์มินิมอล โทนสีม่วง-ขาว พัฒนาด้วย **Next.js 14** และ **MySQL**

## 🎨 คุณสมบัติหลัก

### ✨ ดีไซน์
- **สไตล์มินิมอล** - โทนสีม่วงอ่อน + ขาวพาสเทล
- **Smooth Animations** - เอฟเฟกต์การเปลี่ยนหน้าและ hover ที่นุ่มนวล
- **Responsive Design** - รองรับทุกอุปกรณ์ (มือถือ, แท็บเล็ต, เดสก์ท็อป)
- **Tailwind CSS** - Utility-first CSS framework
- **Framer Motion** - Animation library

### 📋 ฟังก์ชัน

#### 1. หน้าแรก (Home)
- แสดง 4 โซนภูมิภาค: ภาคเหนือ / ใต้ / อีสาน / กลาง
- แสดงรูปภาพและจำนวนที่พักในแต่ละโซน
- คลิกเพื่อดูรายการที่พักในโซนนั้น

#### 2. หน้ารายการที่พัก (Accommodation List)
- แสดงที่พักทั้งหมดในโซนที่เลือก
- การ์ดแสดงรูป, ชื่อ, ราคา, รายละเอียดสั้น
- คลิกเพื่อดูรายละเอียดเต็ม

#### 3. หน้ารายละเอียดที่พัก (Accommodation Details)
- แสดงข้อมูลครบถ้วน: ชื่อ, รายละเอียด, โซน, ราคา/คืน
- แกลเลอรีรูปภาพพร้อมสไลด์โชว์
- สิ่งอำนวยความสะดวก
- ปุ่ม "จองเลย" เปิด Modal จอง

#### 4. ฟอร์มจอง (Booking Modal)
- กรอก: ชื่อ, เบอร์โทร, อีเมล, จำนวนผู้เข้าพัก
- เลือกวันที่เข้าพัก-ออก
- **ตรวจสอบ:** ต้องจองล่วงหน้าอย่างน้อย 2 เดือน
- ตรวจสอบว่าห้องว่างหรือไม่ (ป้องกันการจองซ้อน)
- บันทึกลง MySQL

#### 5. หน้า Dashboard ผู้ใช้
- ค้นหาการจองด้วยอีเมล
- แสดงรายการจองทั้งหมด
- ยกเลิกได้หากเหลือเวลา > 2 เดือนก่อนเข้าพัก
- แสดงสถานะ: ยืนยันแล้ว / ยกเลิกแล้ว / เสร็จสิ้น

#### 6. หน้า Admin
- เพิ่ม/ลบ/แก้ไขข้อมูลที่พัก
- ดูรายการจองทั้งหมด
- จัดการข้อมูลภูมิภาค

## 🗄️ โครงสร้าง Database

### Models (Prisma Schema)

1. **Region** - ภูมิภาค
   - id, name, slug, description, imageUrl

2. **Accommodation** - ที่พัก
   - id, name, slug, description, shortDesc
   - regionId, pricePerNight, maxGuests
   - address, amenities, images

3. **Customer** - ลูกค้า
   - id, fullName, email, phone

4. **Booking** - การจอง
   - id, bookingNumber
   - accommodationId, customerId
   - checkInDate, checkOutDate
   - numberOfGuests, numberOfNights
   - totalPrice, status

## 🚀 การติดตั้ง

### 1. ติดตั้ง Dependencies
```bash
npm install
```

### 2. ตั้งค่า Database
แก้ไขไฟล์ `.env`:
```env
DATABASE_URL="mysql://root:@localhost:3306/exam-claude"
```

### 3. สร้าง Database และ Tables
```bash
npm run prisma:migrate
```

### 4. เพิ่มข้อมูลตัวอย่าง (Seed)
```bash
npm run prisma:seed
```

### 5. เริ่มต้นโปรเจค

**รัน Next.js (Frontend):**
```bash
npm run dev
```
เปิด: http://localhost:3000

**รัน Express API (Backend) - Optional:**
```bash
npm run server:dev
```
API: http://localhost:3001

### 6. เปิด Prisma Studio (Database GUI)
```bash
npm run prisma:studio
```
เปิด: http://localhost:5555

## 📁 โครงสร้างโปรเจค

```
exam-claude/
├── app/                          # Next.js App Directory
│   ├── api/                      # API Routes
│   │   ├── regions/              # API สำหรับภูมิภาค
│   │   ├── accommodations/       # API สำหรับที่พัก
│   │   └── bookings/             # API สำหรับการจอง
│   ├── accommodation/[slug]/     # หน้ารายละเอียดที่พัก
│   ├── region/[slug]/            # หน้ารายการที่พักในโซน
│   ├── dashboard/                # หน้าการจองของผู้ใช้
│   ├── admin/                    # หน้า Admin
│   ├── page.js                   # หน้าแรก
│   └── layout.tsx                # Layout หลัก
├── components/                   # React Components
│   └── BookingModal.js           # Modal สำหรับจองที่พัก
├── lib/                          # Utilities
│   ├── prisma.js                 # Prisma Client (Express)
│   └── prisma-client.js          # Prisma Client (Next.js)
├── prisma/                       # Prisma Configuration
│   ├── schema.prisma             # Database Schema
│   ├── seed.js                   # Seed Script
│   └── migrations/               # Migration Files
├── routes/                       # Express Routes (Optional)
│   └── bookings.js               # Booking Routes
├── server.js                     # Express Server (Optional)
├── examhotel.html                # Static HTML Demo
└── package.json
```

## 🔧 npm Scripts

```bash
# Development
npm run dev              # รัน Next.js development server
npm run server:dev       # รัน Express API server (auto-reload)

# Production
npm run build            # Build Next.js
npm run start            # รัน Next.js production server
npm run server           # รัน Express API server

# Database
npm run prisma:generate  # Generate Prisma Client
npm run prisma:migrate   # รัน database migration
npm run prisma:seed      # เพิ่มข้อมูลตัวอย่าง
npm run prisma:studio    # เปิด Prisma Studio (Database GUI)
```

## 🎯 API Endpoints

### Regions
- `GET /api/regions` - ดึงข้อมูลภูมิภาคทั้งหมด
- `POST /api/regions` - เพิ่มภูมิภาคใหม่

### Accommodations
- `GET /api/accommodations` - ดึงข้อมูลที่พักทั้งหมด (filter ได้)
- `GET /api/accommodations/[slug]` - ดึงข้อมูลที่พักตาม slug
- `POST /api/accommodations` - เพิ่มที่พักใหม่
- `PUT /api/accommodations/[slug]` - แก้ไขที่พัก
- `DELETE /api/accommodations/[slug]` - ลบที่พัก

### Bookings
- `GET /api/bookings` - ดึงข้อมูลการจองทั้งหมด (filter ได้)
- `GET /api/bookings/[id]` - ดึงข้อมูลการจองตาม ID
- `POST /api/bookings` - สร้างการจองใหม่
- `DELETE /api/bookings/[id]` - ยกเลิกการจอง

## 💡 กฎการจองและยกเลิก

### การจอง
✅ ต้องจองล่วงหน้าอย่างน้อย **2 เดือน**
✅ ระบบตรวจสอบวันที่ว่างอัตโนมัติ
✅ ป้องกันการจองซ้อนทับ
✅ คำนวณราคาอัตโนมัติ (จำนวนคืน × ราคาต่อคืน)

### การยกเลิก
✅ ยกเลิกฟรีหากเหลือเวลา **มากกว่า 2 เดือน** ก่อนวันเข้าพัก
❌ ไม่สามารถยกเลิกได้หากเหลือเวลา **น้อยกว่า 2 เดือน**

## 🎨 เทคโนโลยีที่ใช้

### Frontend
- **Next.js 14** - React Framework with App Router
- **Tailwind CSS** - Utility-first CSS
- **Framer Motion** - Animation library
- **React Hooks** - State management

### Backend
- **Next.js API Routes** - Serverless functions
- **Express.js** - API server (optional)
- **Prisma ORM** - Database toolkit
- **MySQL** - Relational database

### Dev Tools
- **TypeScript** - Type safety
- **ESLint** - Code linting
- **Nodemon** - Auto-reload

## 📸 หน้าจอ

1. **หน้าแรก** - แสดงโซนภูมิภาค 4 โซน
2. **รายการที่พัก** - แสดงที่พักในโซนที่เลือก
3. **รายละเอียดที่พัก** - ข้อมูลเต็ม + แกลเลอรี + ฟอร์มจอง
4. **Dashboard** - ค้นหาและจัดการการจองของผู้ใช้
5. **Admin** - จัดการที่พักและดูรายการจอง

## 🔐 ข้อมูลตัวอย่าง (Seed Data)

หลังจากรัน `npm run prisma:seed` ระบบจะเพิ่มข้อมูล:
- **4 ภูมิภาค**: ภาคเหนือ, ใต้, อีสาน, กลาง
- **7 ที่พัก**: กระจายในทุกภูมิภาค
  - รีสอร์ทบนดอย (เชียงใหม่)
  - บ้านไม้ริมน้ำ (เชียงราย)
  - วิลล่าหรูริมทะเล (ภูเก็ต)
  - รีสอร์ทเกาะสมุย
  - โฮมสเตย์อีสาน (นครราชสีมา)
  - คอนโดกรุงเทพ
  - รีสอร์ทริมแม่น้ำ (กาญจนบุรี)

## 🐛 Troubleshooting

### ปัญหา: Prisma Client ไม่อัปเดต
```bash
npm run prisma:generate
```

### ปัญหา: Database ไม่ sync
```bash
npm run prisma:migrate
```

### ปัญหา: Port 3000 ถูกใช้งานแล้ว
แก้ไขใน `package.json`:
```json
"dev": "next dev -p 3001"
```

## 📝 License

ISC License

---

## 🎉 สรุป

เว็บไซต์จองที่พักออนไลน์พร้อมใช้งาน ด้วยฟีเจอร์ครบครัน:
- ✅ ระบบจองที่พักแบบ realtime
- ✅ ตรวจสอบวันที่ว่างอัตโนมัติ
- ✅ ดีไซน์สวยงามสไตล์มินิมอล
- ✅ Responsive ทุกอุปกรณ์
- ✅ Animation ลื่นไหล
- ✅ Admin dashboard
- ✅ ระบบยกเลิกการจอง

**เริ่มต้นใช้งาน:**
```bash
npm install
npm run prisma:migrate
npm run prisma:seed
npm run dev
```

เปิดเบราว์เซอร์ที่ http://localhost:3000 🚀
