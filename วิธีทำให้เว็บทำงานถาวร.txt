========================================================================
  วิธีทำให้ ExamHotel ทำงานถาวร (เปิดคอมทีเว็บทำงานเอง)
========================================================================

🎯 เป้าหมาย:
- เว็บเริ่มอัตโนมัติเมื่อเปิดคอมพิวเตอร์
- ทำงานในเบื้องหลังตลอดเวลา
- Restart อัตโนมัติถ้าเว็บล่ม
- ใช้ URL เดียวกันตลอด: http://192.168.0.130:3000

========================================================================
  ขั้นตอนการติดตั้ง (ทำครั้งเดียว)
========================================================================

ขั้นตอนที่ 1: หยุดเว็บที่รันอยู่
-------------------------------------
1. ถ้ามี Terminal ที่รันเว็บอยู่ ให้กด Ctrl+C เพื่อหยุด


ขั้นตอนที่ 2: เริ่มเว็บด้วย PM2
-------------------------------------
1. เปิด Command Prompt (ไม่ต้อง Admin)
2. ไปที่โฟลเดอร์โปรเจค:
   cd C:\Users\Admin\exam-claude

3. เริ่มเว็บด้วย PM2:
   npx pm2 start npm --name examhotel -- run dev -- --port 3000 --hostname 0.0.0.0

4. รอจนขึ้นข้อความ "online" ในตาราง


ขั้นตอนที่ 3: ตรวจสอบว่าเว็บทำงาน
-------------------------------------
1. ตรวจสอบสถานะ:
   npx pm2 status

2. ดู logs:
   npx pm2 logs examhotel --lines 20

3. ทดสอบเข้าเว็บ:
   - เปิดเบราว์เซอร์ไปที่ http://localhost:3000
   - ถ้าเห็นหน้าเว็บแสดงว่าสำเร็จ ✅


ขั้นตอนที่ 4: บันทึกรายการ PM2
-------------------------------------
1. รันคำสั่ง:
   npx pm2 save

2. จะเห็นข้อความ "Successfully saved"


ขั้นตอนที่ 5: ตั้งค่าเปิดอัตโนมัติตอน boot (สำคัญที่สุด!)
-------------------------------------
⚠️ ขั้นตอนนี้ต้องใช้สิทธิ์ Administrator

**สำหรับ Windows:**

1. เปิด Command Prompt ด้วยสิทธิ์ Administrator:
   - กดปุ่ม Windows
   - พิมพ์ "cmd"
   - คลิกขวาที่ "Command Prompt"
   - เลือก "Run as administrator"

2. รันคำสั่ง:
   cd C:\Users\Admin\exam-claude
   npx pm2-startup install

3. จะเห็นข้อความว่าติดตั้งสำเร็จ

4. รันอีกครั้ง:
   npx pm2 save

5. เสร็จสิ้น! ✅


========================================================================
  ทดสอบว่าตั้งค่าสำเร็จ
========================================================================

1. รีสตาร์ทคอมพิวเตอร์
2. รอ 1-2 นาที
3. เปิดเบราว์เซอร์ไปที่ http://localhost:3000
4. ถ้าเว็บทำงานแสดงว่าสำเร็จ! 🎉


========================================================================
  คำสั่งจัดการเว็บด้วย PM2
========================================================================

ดูสถานะ:
  npx pm2 status

ดู logs (ข้อความจากเว็บ):
  npx pm2 logs examhotel
  npx pm2 logs examhotel --lines 100

รีสตาร์ทเว็บ:
  npx pm2 restart examhotel

หยุดเว็บ:
  npx pm2 stop examhotel

เริ่มเว็บใหม่:
  npx pm2 start examhotel

ลบเว็บออกจาก PM2:
  npx pm2 delete examhotel

ดูการใช้ทรัพยากร:
  npx pm2 monit


========================================================================
  URL สำหรับเข้าใช้งาน
========================================================================

จากคอมพิวเตอร์:
  http://localhost:3000
  http://192.168.0.130:3000

จากโทรศัพท์มือถือ (WiFi เดียวกัน):
  http://192.168.0.130:3000


========================================================================
  แก้ปัญหา
========================================================================

❌ ปัญหา: เว็บไม่เริ่มตอน boot

✅ วิธีแก้:
1. เปิด Command Prompt (Admin)
2. ไปที่โฟลเดอร์โปรเจค:
   cd C:\Users\Admin\exam-claude

3. รันคำสั่งนี้:
   npx pm2-startup install
   npx pm2 save

4. รีสตาร์ทคอมพิวเตอร์และทดสอบใหม่


❌ ปัญหา: PM2 command not found

✅ วิธีแก้:
1. ติดตั้ง PM2:
   npm install -g pm2

2. ลองอีกครั้ง


❌ ปัญหา: เว็บไม่ทำงานหลังรีสตาร์ท

✅ วิธีแก้:
1. เช็คว่า PM2 รันอยู่หรือไม่:
   npx pm2 status

2. ถ้าไม่มีรายการ ให้รันใหม่:
   cd C:\Users\Admin\exam-claude
   npx pm2 start npm --name examhotel -- run dev -- --port 3000 --hostname 0.0.0.0
   npx pm2 save

3. ตั้งค่า startup ใหม่:
   npx pm2-startup install (ต้อง Admin)


========================================================================
  สรุป: Quick Start
========================================================================

ทำครั้งเดียว:
  1. cd C:\Users\Admin\exam-claude
  2. npx pm2 start npm --name examhotel -- run dev -- --port 3000 --hostname 0.0.0.0
  3. npx pm2 save
  4. เปิด CMD (Admin): npx pm2-startup install
  5. รีสตาร์ทคอมพิวเตอร์เพื่อทดสอบ

หลังจากนั้น:
  - เว็บจะเปิดอัตโนมัติทุกครั้งที่เปิดคอม
  - เข้าได้ที่ http://localhost:3000
  - จากมือถือ: http://192.168.0.130:3000


========================================================================
  ข้อดี
========================================================================

✅ เปิดคอมทีเว็บทำงานเอง
✅ ปิด Terminal ได้ เว็บยังทำงาน
✅ Restart อัตโนมัติถ้าล่ม
✅ จัดการง่ายด้วย PM2
✅ ดู logs ได้ทุกเวลา
✅ ใช้ทรัพยากรน้อย


========================================================================
  วิธีถอนการติดตั้ง (ถ้าต้องการ)
========================================================================

1. เปิด Command Prompt (Admin)
2. รัน:
   npx pm2-startup uninstall
   npx pm2 delete examhotel
   npx pm2 save


========================================================================

© 2024 ExamHotel Team
เว็บไซต์จองที่พักออนไลน์สไตล์มินิมอล

========================================================================
